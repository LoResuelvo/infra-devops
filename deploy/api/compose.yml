services:
  api:
    image: ${IMAGE_REF:?IMAGE_REF is required}
    env_file:
      - path: /etc/loresuelvo/api/api.env
        format: raw
    ports:
      - "127.0.0.1:8080:8080"
    restart: unless-stopped
    networks:
      - app-network

  migrate:
    image: ${IMAGE_REF:?IMAGE_REF is required}
    profiles:
      - tools
    env_file:
      - path: /etc/loresuelvo/api/api.env
        format: raw
    entrypoint: ["/bin/sh", "-ec"]
    command: ['exec /usr/local/bin/migrate -path /migrations -database "$$DATABASE_URL" up']
    networks:
      - app-network

networks:
  app-network:
    name: app-network
    external: true
